<div class="page-header">
	<div class="row">
		<div class="col-lg-9">
			<h1>Categories</h1>
		</div>

		<% if is_admin? then %>
			<%= render 'form' %>
		<% end %>
	</div> 
</div>

<div class="row">
	<%= render @categories %>
</div>
