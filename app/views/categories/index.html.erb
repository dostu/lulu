<div class="page-header">
	<h1>Categories</h1>
</div>

<% if is_admin? then %>
	<%= link_to "Add Category", new_category_path, :class => "btn btn-success" %>
<% end %> 

<div class="row">
	<%= render @categories %>
</div>
